replicaCount: 1

image:
  repository: quyenlc/harness-turtorial
  tag: <+pipeline.sequenceId>
  pullPolicy: IfNotPresent

service:
  enabled: true
  name: custom-nginx-service
  type: LoadBalancer
  port: 80
  targetPort: 80

resources: {}
